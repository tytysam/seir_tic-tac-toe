// Cached DOM Nodes
const grid = document.querySelectorAll(".tile");
const emptyBox = document.querySelectorAll(".display");
const header = document.querySelector("h2");
const reset = document.getElementById("reset-button");
